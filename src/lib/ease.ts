/** Shared motion vocabulary — one easing language across the whole site. */
export const EASE = [0.16, 1, 0.3, 1] as const; // expo-out: decisive, settles softly
export const EASE_IO = [0.76, 0, 0.24, 1] as const; // hard in-out: curtains, flips
export const DUR = { fast: 0.5, base: 0.9, slow: 1.4 } as const;
